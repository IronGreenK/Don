import './App.css';

// Pantalla provisional: la UI real se portará desde el prototipo el-don.html
// cuando el archivo esté disponible en el repositorio.
function App() {
  return (
    <main className="app-shell">
      <h1>El Don</h1>
      <p>Proyecto base listo: React + Vite + Capacitor + Supabase.</p>
      <p className="pending">Pendiente: portar la UI del prototipo el-don.html.</p>
    </main>
  );
}

export default App;
